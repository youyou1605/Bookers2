<%= render 'books/list', user: @user, book: @book%>




<h1>Users</h1>
<% @users.each do |user| %>
<div>
   
  <p>name <%= user.name %></p>
  <%= link_to "Show", user_path(user.id) %>
</div>
<% end %>